name: my_first_test

on: 
  push:
    branches: 
      - master

jobs:
  my_testing: 
    runs-on: ubuntu-latest
    steps:
      - name: print hello
        run: echo "hello world from testing job" 
        
  my_testing2:
    runs-on: ubuntu-latest
    steps:
        - name: print hello
          run: echo "hello world from deploy job"
